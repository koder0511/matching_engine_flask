This is a matching engine for matching resume PDF to job description entered on the front end, it analyzes the resume and gives match percentage. Modules used are PyMuPDF,
flask, PIL, python-dotenv, os, io, re, base64, google-generativeai. Gemini is used as Google API key is used. 
